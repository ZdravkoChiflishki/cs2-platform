[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cs2-platform"
version = "0.1.0"
description = "Python-first CS2 server platform runtime and tooling"
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[project.scripts]
cs2-server = "cs2_server.__main__:main"
cs2-validate-mode = "cs2_tools.validate_mode:main"
cs2-query-server = "cs2_tools.query_server:main"
cs2-smoke = "cs2_tools.smoke:main"
cs2-generate-server = "cs2_tools.server_generator:main"
cs2-validate-plugins = "cs2_tools.plugin_manifest:main"
cs2-production-check = "cs2_tools.production_check:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
